<div class="collection-empty">
  <span class="collection-empty-icon"><%= admin_icon(icon) %></span>
  <strong><%= title %></strong>
  <p><%= text %></p>
  <% if local_assigns[:action_path] %><%= link_to action_label, action_path, class: "admin-button is-primary" %><% end %>
</div>
