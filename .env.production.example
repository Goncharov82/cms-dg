# This file is a template. The real production file lives only at
# ~/app/shared/.env on the server and must never be committed.
RAILS_MASTER_KEY=copy_from_config_master_key
SECRET_KEY_BASE=generate_with_bin_rails_secret

DATABASE_NAME=goncharoff_pro_production
DATABASE_CACHE_NAME=goncharoff_pro_production_cache
DATABASE_QUEUE_NAME=goncharoff_pro_production_queue
DATABASE_CABLE_NAME=goncharoff_pro_production_cable
DATABASE_USERNAME=goncharoff
DATABASE_PASSWORD=generate_a_long_random_password
DATABASE_PORT=5432

APP_HOST=test.goncharoff.pro
APP_TIME_ZONE=Europe/Moscow
APP_BIND_ADDRESS=127.0.0.1
APP_PORT=3090

RAILS_LOG_LEVEL=info
RAILS_MAX_THREADS=5
WEB_CONCURRENCY=1

# Used only on the first intentional seed run.
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=replace_before_first_seed
